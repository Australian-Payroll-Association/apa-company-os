.DS_Store
node_modules/
.next/
out/
.vercel
.env.local

# Local working assets (source photos, not served)
context/

# Project instructions are kept local, not version-controlled
CLAUDE.md

# Claude Code worktrees are local scratch checkouts, never committed
.claude/worktrees/
